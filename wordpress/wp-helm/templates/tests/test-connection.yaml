apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wp-helm.fullname" . }}-test-connection"
  labels:
    {{- include "wp-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wp-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
